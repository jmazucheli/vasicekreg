% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dpqr-HSvasicekQ.R
\name{HSVASIQ}
\alias{HSVASIQ}
\alias{dHSVASIQ}
\alias{pHSVASIQ}
\alias{qHSVASIQ}
\alias{rHSVASIQ}
\title{Hyperbolic-secant-kernel Vasicek quantile distribution}
\usage{
dHSVASIQ(x, mu, sigma, tau = 0.5, log = FALSE)

pHSVASIQ(q, mu, sigma, tau = 0.5, lower.tail = TRUE, log.p = FALSE)

qHSVASIQ(p, mu, sigma, tau = 0.5, lower.tail = TRUE, log.p = FALSE)

rHSVASIQ(n, mu, sigma, tau = 0.5)

HSVASIQ(mu.link = "logit", sigma.link = "logit")
}
\arguments{
\item{x, q}{Vector of quantiles in \eqn{(0,1)}.}

\item{mu}{Vector of conditional \eqn{\tau}-th quantiles,
\eqn{0<\mu<1}.}

\item{sigma}{Vector of dispersion values, \eqn{0<\sigma<1}.}

\item{tau}{Scalar in \eqn{(0,1)} fixing the quantile represented by
\eqn{\mu}. For \code{HSVASIQ()}, it must be defined globally.}

\item{log}{Logical; if \code{TRUE}, returns the log-density.}

\item{lower.tail}{Logical; if \code{TRUE}, probabilities are
\eqn{P(X\leq x)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are supplied or
returned on the logarithmic scale.}

\item{p}{Vector of probabilities.}

\item{n}{Number of observations.}

\item{mu.link}{Link function for \eqn{\mu}.}

\item{sigma.link}{Link function for \eqn{\sigma}.}
}
\value{
\code{dHSVASIQ()} returns the density, \code{pHSVASIQ()} the distribution
function, \code{qHSVASIQ()} the quantile function, and \code{rHSVASIQ()}
random deviates. \code{HSVASIQ()} returns a \code{gamlss.family} object.
}
\description{
\code{HSVASIQ()} defines the hyperbolic-secant-kernel Vasicek distribution
as a \code{gamlss.family} object for conditional quantile regression.
}
\details{
Let
\deqn{H(w)=\frac{2}{\pi}\arctan\{\exp(w)\},\qquad
h(w)=\frac{1}{\pi\cosh(w)},}
and
\deqn{q(u)=H^{-1}(u)=
\log\left\{\tan\left(\frac{\pi u}{2}\right)\right\}.}
For fixed \eqn{\tau\in(0,1)}, define
\deqn{a=\sqrt{\frac{1-\sigma}{\sigma}},\qquad
z=a\{q(x)-q(\mu)\}+q(\tau).}
The cumulative distribution function and density are
\deqn{F(x\mid\mu,\sigma,\tau)=H(z)}
and
\deqn{f(x\mid\mu,\sigma,\tau)=
a\frac{h(z)}{h\{q(x)\}}.}
The quantile function is
\deqn{Q(p\mid\mu,\sigma,\tau)=
H\left[q(\mu)+\sqrt{\frac{\sigma}{1-\sigma}}
\{q(p)-q(\tau)\}\right].}
Consequently, \eqn{Q(\tau)=\mu}; \eqn{\mu} is the conditional
\eqn{\tau}-th quantile and is not the conditional mean in general.

The GAMLSS family uses analytical first, second, and cross derivatives.
The conditional mean and variance are evaluated by numerical integration
of the quantile function.

For GAMLSS fitting, \code{tau} must be defined as a scalar variable in the
global environment before \code{HSVASIQ()} is evaluated and must retain the
same value for post-fit calculations.
}
\examples{
set.seed(123)
y <- rHSVASIQ(500, mu = 0.60, sigma = 0.30, tau = 0.25)

tau <- 0.25
fit <- gamlss::gamlss(
    y ~ 1,
    sigma.formula = ~ 1,
    family = HSVASIQ(),
    control = gamlss::gamlss.control(trace = FALSE)
)
fitted(fit, what = "mu")[1]
rm(tau)
}
\references{
Dunn, P. K. and Smyth, G. K. (1996). Randomized quantile residuals.
\emph{Journal of Computational and Graphical Statistics}, \bold{5}(3),
236--244.

Mazucheli, J., Alves, B., Korkmaz, M. C. and Leiva, V. (2022).
Vasicek quantile and mean regression models for bounded data:
New formulation, mathematical derivations, and numerical applications.
\emph{Mathematics}, \bold{10}, 1389.
\doi{10.3390/math10091389}
}
\author{
Josmar Mazucheli \email{jmazucheli@gmail.com}
}
